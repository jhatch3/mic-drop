services:
  - type: web
    name: pitch-battle-server
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: PORT
        value: 3001
    healthCheckPath: /health
